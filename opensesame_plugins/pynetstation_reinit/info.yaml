description: PyNetStation Re-Initialization.
priority: 20
controls:
  - type: "text"
    label: "This item re-initializes Net Station. It is necessary if you end an EEG recording session prior to ending the experiment. Make sure you have a new session started before loading this item, otherwise the experiment will crash."
